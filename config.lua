-- centralised gameplay configuration
local config = {
    VERSION = "0.1.0",
    CAMERA_MIN = 4000,
    CAMERA_MAX = 96000,
    SHIP_SPEED = 300,        -- base ship speed
    ANIM_FRAME_TIME = 0.125, -- seconds per frame (8 FPS)
    CAMERA_SMOOTHING = 5     -- camera acceleration factor
}

return config

